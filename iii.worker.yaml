name: spec-to-worker
description: Convert OpenAPI specs and MCP servers into triggerable iii functions.
language: rust
entrypoint: cargo run --bin spec-to-worker -- serve
functions:
  - spec-to-worker::convert
